__pycache__/
*.py[cod]
.venv/
.env
# Downloaded by scripts/download_models.py
models/
# Enrolled face photos are personal data; keep them on the Pi only.
faces/
# Local settings such as the alert mode.
settings.json
# Authorised RFID cards (names and card IDs).
cards.json
